define([
], function(){

    var config = {
        exports: {
            'export.download.geojson': 'GeoJSON',
            'export.google.maps': 'Google Maps'
        }
    };

    return config;
});