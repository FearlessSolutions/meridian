var expect = chai.expect;
 

describe("Placeholder Test Suite", function() {
  describe("Placeholder Tests", function() {
    it("should think 1 is equal to 1", function() {
      // expect(cow.name).to.equal("Meridian Map");
      // expect($('#rating').data('barrating')).to.be.a('object');
      expect(1).to.equal(1);
    });
    it("banners should think 4/2 = 2", function() {
        expect(4/2).to.equal(2);
    });
  });
});